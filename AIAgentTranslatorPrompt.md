本文档作为提供给AI Agent进行翻译的要求，AI Agent在处理上下文时**不要**将此文档内容压缩。

AI完成翻译时应遵循以下TODO List，依次完成:

1. 环境检查: 执行`deploy/generate.sh`脚本，确保无任何输出, 有输出即退出, 要求人工介入以修复环境。
2. 列出文档下所有`{include \d 子文档}`命令: 该命令会让该文档包含指定的子文档内容，**Agent也应该在该子文档下执行除了环境检查外的全部步骤**。
3. 翻译每一篇文档:
   - 参考`words.txt`中的翻译表进行翻译, 对于未在表中列出的词语, 使用符合lean4语境的翻译, 对于比较重要的翻译，将其加入words.txt中。
   - 保持原代码格式, 原文不是markdown，所以不要修改任何符号
   - 翻译前先将英文内容以 `--`  注释掉，然后再翻译， 注释时整段整段的注释,然后在整段注释的后边添加翻译,而不是按行
   - 遇到`{tech}[xxx]`，将其改成 `{tech key := "xxx"}[中文]` 的格式 , 一定要添加 `key := "xxx"`
   - 遇到`{deftech}_xxx_` 时，修改为 `{deftech key := "xxx"}_中文_`, 一定要添加 `key := "xxx"`
   - 如果原文中没有使用`{tech}`或`{deftech}`，则不要添加
   - `{index}[xxx]` 不需要翻译
   - 以 `:::`/`::::`/`:::::` 包裹的代码块是一个整体， 需要整体注释掉重新创建一个新的翻译过的版本, 该结构是嵌套的即`:::::`可以包裹`::::`, 翻译时需要注释掉全部的内容
   - 如下#doc 对应的%%%块下需要添加`file := 原文件英文名`和`tag := 小写英文名以-分割`，如果原始代码中已有则不需要添加
    ```
    #doc (Manual) "简介" =>
    %%%
    file := "Introduction"
    %%%
    ```
4. 校对: 对于每一篇文档，依次检查
   - 翻译是否满足步骤3中的要求
   - 是否存在漏翻/多翻译的情况: 部分大模型会漏翻长句会整段话中的某些部分, 必须对每句话进行检查
   - 翻译的一致性: 检查翻译结果中对某些名词的翻译是否一致，比如constructor 大部分情况会被翻译成 `构造器` 或 `构造函数`, 但是lean的语境下应翻译成`构造子`
   - 语句的通顺: AI有时会为了保证翻译的准确，翻译出的句子很难理解。
5. 执行`deploy/generate.sh`脚本，确保无任何输出