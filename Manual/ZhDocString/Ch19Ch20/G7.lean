/-
Copyright (c) 2026 Lean 中文社区. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Lean 中文社区
-/
import Manual.ZhDocString.ZhDocString

namespace Manual.ZhDocString.Ch19Ch20.G7

set_option linter.unusedVariables false
set_option autoImplicit true

/-!
本模块为第 19–20 章的定精度整数、单元类型与布尔 API 提供中文动态文档载体。
普通定义直接别名到真实声明；结构体和归纳类型按真实构造子与字段逐项镜像。
-/

/--
平台架构字长的无符号整数。

在 32 位架构上，`USize` 等价于 `UInt32`。在 64 位机器上，它等价于 `UInt64`。
-/
structure c001 where
  ofBitVec ::
    /--
  将 `USize` 解包为 `BitVec System.Platform.numBits`。此函数会被原生实现覆盖。
  -/
  toBitVec : BitVec System.Platform.numBits

/--
从 `BitVec System.Platform.numBits` 创建 `USize`。此函数会被原生实现覆盖。
-/
add_decl_doc c001.ofBitVec

/--
无符号 8 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 8 位值，而不必包装 `BitVec 8`。
-/
structure c002 where
  ofBitVec ::
    /--
  将 `UInt8` 解包为 `BitVec 8`。此函数会被原生实现覆盖。
  -/
  toBitVec : BitVec 8

/--
从 `BitVec 8` 创建 `UInt8`。此函数会被原生实现覆盖。
-/
add_decl_doc c002.ofBitVec

/--
无符号 16 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 16 位值，而不必包装 `BitVec 16`。
-/
structure c003 where
  ofBitVec ::
    /--
  将 `UInt16` 解包为 `BitVec 16`。此函数会被原生实现覆盖。
  -/
  toBitVec : BitVec 16

/--
从 `BitVec 16` 创建 `UInt16`。此函数会被原生实现覆盖。
-/
add_decl_doc c003.ofBitVec

/--
无符号 32 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 32 位值，而不必包装 `BitVec 32`。
-/
structure c004 where
  ofBitVec ::
    /--
  将 `UInt32` 解包为 `BitVec 32`。此函数会被原生实现覆盖。
  -/
  toBitVec : BitVec 32

/--
从 `BitVec 32` 创建 `UInt32`。此函数会被原生实现覆盖。
-/
add_decl_doc c004.ofBitVec

/--
无符号 64 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 64 位值，而不必包装 `BitVec 64`。
-/
structure c005 where
  ofBitVec ::
    /--
  将 `UInt64` 解包为 `BitVec 64`。此函数会被原生实现覆盖。
  -/
  toBitVec : BitVec 64

/--
从 `BitVec 64` 创建 `UInt64`。此函数会被原生实现覆盖。
-/
add_decl_doc c005.ofBitVec

/--
平台架构字长的有符号整数。

在 32 位架构上，`ISize` 等价于 `Int32`。在 64 位机器上，它等价于 `Int64`。编译器对此类型提供特殊支持，因此它可以表示为未装箱的值。
-/
structure c006 where
  ofUSize ::
    /--
  将平台字长有符号整数转换为作为其二进制补码编码的平台字长无符号整数。
  -/
  toUSize : USize

/--
有符号 8 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 8 位值。
-/
structure c007 where
  ofUInt8 ::
    /--
  将 8 位有符号整数转换为作为其二进制补码编码的 8 位无符号整数。
  -/
  toUInt8 : UInt8

/--
有符号 16 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 16 位值。
-/
structure c008 where
  ofUInt16 ::
    /--
  将 16 位有符号整数转换为作为其二进制补码编码的 16 位无符号整数。
  -/
  toUInt16 : UInt16

/--
有符号 32 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 32 位值。
-/
structure c009 where
  ofUInt32 ::
    /--
  将 32 位有符号整数转换为作为其二进制补码编码的 32 位无符号整数。
  -/
  toUInt32 : UInt32

/--
有符号 64 位整数。

编译器对此类型提供特殊支持，因此它可以表示为未装箱的 64 位值。
-/
structure c010 where
  ofUInt64 ::
    /--
  将 64 位有符号整数转换为作为其二进制补码编码的 64 位无符号整数。
  -/
  toUInt64 : UInt64

/--
`USize` 可表示的不同值的数量，即 `2^System.Platform.numBits`。
-/
def c011 := @USize.size

/--
`ISize` 可表示的不同值的数量，即 `2^System.Platform.numBits`。
-/
def c012 := @ISize.size

/--
`UInt8` 可表示的不同值的数量，即 `2^8 = 256`。
-/
def c013 := @UInt8.size

/--
`Int8` 可表示的不同值的数量，即 `2^8 = 256`。
-/
def c014 := @Int8.size

/--
`UInt16` 可表示的不同值的数量，即 `2^16 = 65536`。
-/
def c015 := @UInt16.size

/--
`Int16` 可表示的不同值的数量，即 `2^16 = 65536`。
-/
def c016 := @Int16.size

/--
`UInt32` 可表示的不同值的数量，即 `2^32 = 4294967296`。
-/
def c017 := @UInt32.size

/--
`Int32` 可表示的不同值的数量，即 `2^32 = 4294967296`。
-/
def c018 := @Int32.size

/--
`UInt64` 可表示的不同值的数量，即 `2^64 = 18446744073709551616`。
-/
def c019 := @UInt64.size

/--
`Int64` 可表示的不同值的数量，即 `2^64 = 18446744073709551616`。
-/
def c020 := @Int64.size

/--
`ISize` 可表示的最小数：`-2^(System.Platform.numBits - 1)`。
-/
def c021 := @ISize.minValue

/--
`ISize` 可表示的最大数：`2^(System.Platform.numBits - 1) - 1`。
-/
def c022 := @ISize.maxValue

/--
`Int8` 可表示的最小数：`-2^7 = -128`。
-/
def c023 := @Int8.minValue

/--
`Int8` 可表示的最大数：`2^7 - 1 = 127`。
-/
def c024 := @Int8.maxValue

/--
`Int16` 可表示的最小数：`-2^15 = -32768`。
-/
def c025 := @Int16.minValue

/--
`Int16` 可表示的最大数：`2^15 - 1 = 32767`。
-/
def c026 := @Int16.maxValue

/--
`Int32` 可表示的最小数：`-2^31 = -2147483648`。
-/
def c027 := @Int32.minValue

/--
`Int32` 可表示的最大数：`2^31 - 1 = 2147483647`。
-/
def c028 := @Int32.maxValue

/--
`Int64` 可表示的最小数：`-2^63 = -9223372036854775808`。
-/
def c029 := @Int64.minValue

/--
`Int64` 可表示的最大数：`2^63 - 1 = 9223372036854775807`。
-/
def c030 := @Int64.maxValue

/--
将平台字长有符号整数转换为表示同一数值的任意精度整数。

此函数在运行时会被高效实现覆盖。
-/
def c031 := @ISize.toInt

/--
将8 位有符号整数转换为表示同一数值的任意精度整数。

此函数在运行时会被高效实现覆盖。
-/
def c032 := @Int8.toInt

/--
将16 位有符号整数转换为表示同一数值的任意精度整数。

此函数在运行时会被高效实现覆盖。
-/
def c033 := @Int16.toInt

/--
将32 位有符号整数转换为表示同一数值的任意精度整数。

此函数在运行时会被高效实现覆盖。
-/
def c034 := @Int32.toInt

/--
将64 位有符号整数转换为表示同一数值的任意精度整数。

此函数在运行时会被高效实现覆盖。
-/
def c035 := @Int64.toInt

/--
将任意精度整数转换为平台字长有符号整数；发生上溢或下溢时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c036 := @ISize.ofInt

/--
将任意精度整数转换为8 位有符号整数；发生上溢或下溢时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `Int8.ofInt 48 = 48`
* `Int8.ofInt (-115) = -115`
* `Int8.ofInt (-129) = 127`
* `Int8.ofInt (128) = -128`
-/
def c037 := @Int8.ofInt

/--
将任意精度整数转换为16 位有符号整数；发生上溢或下溢时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `Int16.ofInt 48 = 48`
* `Int16.ofInt (-129) = -129`
* `Int16.ofInt (128) = 128`
* `Int16.ofInt 70000 = 4464`
* `Int16.ofInt (-40000) = 25536`
-/
def c038 := @Int16.ofInt

/--
将任意精度整数转换为32 位有符号整数；发生上溢或下溢时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `Int32.ofInt 48 = 48`
* `Int32.ofInt (-129) = -129`
* `Int32.ofInt 70000 = 70000`
* `Int32.ofInt (-40000) = -40000`
* `Int32.ofInt 2147483648 = -2147483648`
* `Int32.ofInt (-2147483649) = 2147483647`
-/
def c039 := @Int32.ofInt

/--
将任意精度整数转换为64 位有符号整数；发生上溢或下溢时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `Int64.ofInt 48 = 48`
* `Int64.ofInt (-40_000) = -40_000`
* `Int64.ofInt 2_147_483_648 = 2_147_483_648`
* `Int64.ofInt (-2_147_483_649) = -2_147_483_649`
* `Int64.ofInt 9_223_372_036_854_775_808 = -9_223_372_036_854_775_808`
* `Int64.ofInt (-9_223_372_036_854_775_809) = 9_223_372_036_854_775_807`
-/
def c040 := @Int64.ofInt

/--
构造 `ISize`，输入为 `Int`；若值过小或过大，则将其钳制到可表示范围内。
-/
def c041 := @ISize.ofIntClamp

/--
构造 `Int8`，输入为 `Int`；若值过小或过大，则将其钳制到可表示范围内。
-/
def c042 := @Int8.ofIntClamp

/--
构造 `Int16`，输入为 `Int`；若值过小或过大，则将其钳制到可表示范围内。
-/
def c043 := @Int16.ofIntClamp

/--
构造 `Int32`，输入为 `Int`；若值过小或过大，则将其钳制到可表示范围内。
-/
def c044 := @Int32.ofIntClamp

/--
构造 `Int64`，输入为 `Int`；若值过小或过大，则将其钳制到可表示范围内。
-/
def c045 := @Int64.ofIntClamp

/--
构造 `ISize`，输入为已知位于范围内的 `Int`。
-/
def c046 := @ISize.ofIntLE

/--
构造 `Int8`，输入为已知位于范围内的 `Int`。
-/
def c047 := @Int8.ofIntLE

/--
构造 `Int16`，输入为已知位于范围内的 `Int`。
-/
def c048 := @Int16.ofIntLE

/--
构造 `Int32`，输入为已知位于范围内的 `Int`。
-/
def c049 := @Int32.ofIntLE

/--
构造 `Int64`，输入为已知位于范围内的 `Int`。
-/
def c050 := @Int64.ofIntLE

/--
将任意精度自然数转换为平台字长无符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c051 := @USize.ofNat

/--
将任意精度自然数转换为平台字长有符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c052 := @ISize.ofNat

/--
将自然数转换为8 位无符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `UInt8.ofNat 5 = 5`
* `UInt8.ofNat 255 = 255`
* `UInt8.ofNat 256 = 0`
* `UInt8.ofNat 259 = 3`
* `UInt8.ofNat 32770 = 2`
-/
def c053 := @UInt8.ofNat

/--
将自然数转换为8 位有符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `Int8.ofNat 53 = 53`
* `Int8.ofNat 127 = 127`
* `Int8.ofNat 128 = -128`
* `Int8.ofNat 255 = -1`
-/
def c054 := @Int8.ofNat

/--
将自然数转换为16 位无符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `UInt16.ofNat 5 = 5`
* `UInt16.ofNat 255 = 255`
* `UInt16.ofNat 32770 = 32770`
* `UInt16.ofNat 65537 = 1`
-/
def c055 := @UInt16.ofNat

/--
将自然数转换为16 位有符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `Int16.ofNat 127 = 127`
* `Int16.ofNat 32767 = 32767`
* `Int16.ofNat 32768 = -32768`
* `Int16.ofNat 32770 = -32766`
-/
def c056 := @Int16.ofNat

/--
将自然数转换为32 位无符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `UInt32.ofNat 5 = 5`
* `UInt32.ofNat 65539 = 65539`
* `UInt32.ofNat 4_294_967_299 = 3`
-/
def c057 := @UInt32.ofNat

/--
将自然数转换为32 位有符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `Int32.ofNat 127 = 127`
* `Int32.ofNat 32770 = 32770`
* `Int32.ofNat 2_147_483_647 = 2_147_483_647`
* `Int32.ofNat 2_147_483_648 = -2_147_483_648`
-/
def c058 := @Int32.ofNat

/--
将自然数转换为64 位无符号整数；发生溢出时回绕。

此函数在运行时会被高效实现覆盖。

示例：
* `UInt64.ofNat 5 = 5`
* `UInt64.ofNat 65539 = 65539`
* `UInt64.ofNat 4_294_967_299 = 4_294_967_299`
* `UInt64.ofNat 18_446_744_073_709_551_620 = 4`
-/
def c059 := @UInt64.ofNat

/--
将自然数转换为 64 位有符号整数；发生溢出时回绕为负数。

此函数在运行时会被高效实现覆盖。

示例：
 * `Int64.ofNat 127 = 127`
 * `Int64.ofNat 2_147_483_648 = 2_147_483_648`
 * `Int64.ofNat 9_223_372_036_854_775_807 = 9_223_372_036_854_775_807`
 * `Int64.ofNat 9_223_372_036_854_775_808 = -9_223_372_036_854_775_808`
 * `Int64.ofNat 18_446_744_073_709_551_618 = 0`
-/
def c060 := @Int64.ofNat

/--
将自然数转换为 `USize`。在任何受支持的平台上都不可能溢出，因为 `USize.size` 为 `2^32` 或 `2^64`。

此函数在运行时会被高效实现覆盖。
-/
def c061 := @USize.ofNat32

/--
将自然数转换为 `USize`。需要证明该数足够小，因而可以无溢出地表示。

此函数在运行时会被高效实现覆盖。
-/
def c062 := @USize.ofNatLT

/--
将自然数转换为 `2^8`。需要证明该数足够小，因而可以无溢出地表示。

此函数在运行时会被高效实现覆盖。
-/
def c063 := @UInt8.ofNatLT

/--
将自然数转换为 `2^16`。需要证明该数足够小，因而可以无溢出地表示。

此函数在运行时会被高效实现覆盖。
-/
def c064 := @UInt16.ofNatLT

/--
将自然数转换为 `2^32`。需要证明该数足够小，因而可以无溢出地表示。

此函数在运行时会被高效实现覆盖。
-/
def c065 := @UInt32.ofNatLT

/--
将自然数转换为 `2^64`。需要证明该数足够小，因而可以无溢出地表示。

此函数在运行时会被高效实现覆盖。
-/
def c066 := @UInt64.ofNatLT

/--
将自然数转换为 `USize`；若该数过大，则返回可表示的最大值。

返回 `USize.size - 1`；依平台而定，该值为 `2^64 - 1` 或 `2^32 - 1`。这适用于大于等于 `USize.size` 的自然数。
-/
def c067 := @USize.ofNatClamp

/--
将自然数转换为8 位无符号整数；若该数过大，则返回可表示的最大值。

对于过大的自然数返回 `2^8 - 1`；这适用于大于等于 `2^8` 的自然数。
-/
def c068 := @UInt8.ofNatClamp

/--
将自然数转换为16 位无符号整数；若该数过大，则返回可表示的最大值。

对于过大的自然数返回 `2^16 - 1`；这适用于大于等于 `2^16` 的自然数。
-/
def c069 := @UInt16.ofNatClamp

/--
将自然数转换为32 位无符号整数；若该数过大，则返回可表示的最大值。

对于过大的自然数返回 `2^32 - 1`；这适用于大于等于 `2^32` 的自然数。
-/
def c070 := @UInt32.ofNatClamp

/--
将自然数转换为64 位无符号整数；若该数过大，则返回可表示的最大值。

对于过大的自然数返回 `2^64 - 1`；这适用于大于等于 `2^64` 的自然数。
-/
def c071 := @UInt64.ofNatClamp

/--
将平台字长无符号整数转换为任意精度自然数。

此函数在运行时会被高效实现覆盖。
-/
def c072 := @USize.toNat

/--
将平台字长有符号整数转换为自然数，并将所有负数映射为 `0`。

若要获得二进制补码表示，请使用 `ISize.toBitVec`。
-/
def c073 := @ISize.toNatClampNeg

/--
将8 位无符号整数转换为任意精度自然数。

此函数在运行时会被高效实现覆盖。
-/
def c074 := @UInt8.toNat

/--
将8 位有符号整数转换为自然数，并将所有负数映射为 `0`。

若要获得二进制补码表示，请使用 `Int8.toBitVec`。
-/
def c075 := @Int8.toNatClampNeg

/--
将16 位无符号整数转换为任意精度自然数。

此函数在运行时会被高效实现覆盖。
-/
def c076 := @UInt16.toNat

/--
将16 位有符号整数转换为自然数，并将所有负数映射为 `0`。

若要获得二进制补码表示，请使用 `Int16.toBitVec`。
-/
def c077 := @Int16.toNatClampNeg

/--
将32 位无符号整数转换为任意精度自然数。

此函数在运行时会被高效实现覆盖。
-/
def c078 := @UInt32.toNat

/--
将32 位有符号整数转换为自然数，并将所有负数映射为 `0`。

若要获得二进制补码表示，请使用 `Int32.toBitVec`。
-/
def c079 := @Int32.toNatClampNeg

/--
将64 位无符号整数转换为任意精度自然数。

此函数在运行时会被高效实现覆盖。
-/
def c080 := @UInt64.toNat

/--
将64 位有符号整数转换为自然数，并将所有负数映射为 `0`。

若要获得二进制补码表示，请使用 `Int64.toBitVec`。
-/
def c081 := @Int64.toNatClampNeg

/--
将平台字长无符号整数转换为8 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c082 := @USize.toUInt8

/--
将平台字长无符号整数转换为16 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c083 := @USize.toUInt16

/--
将平台字长无符号整数转换为32 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c084 := @USize.toUInt32

/--
将平台字长无符号整数转换为 32 位无符号整数。由于 `USize.size` 为 `2^32` 或 `2^64`，此转换不会溢出。

此函数在运行时会被高效实现覆盖。
-/
def c085 := @USize.toUInt64

/--
得到 `ISize`，它与 `USize` 二进制补码等价。
-/
def c086 := @USize.toISize

/--
得到 `Int8`，它与 `UInt8` 二进制补码等价。
-/
def c087 := @UInt8.toInt8

/--
将8 位无符号整数转换为16 位无符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c088 := @UInt8.toUInt16

/--
将8 位无符号整数转换为32 位无符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c089 := @UInt8.toUInt32

/--
将8 位无符号整数转换为64 位无符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c090 := @UInt8.toUInt64

/--
将8 位无符号整数转换为平台字长无符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c091 := @UInt8.toUSize

/--
将16 位无符号整数转换为8 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c092 := @UInt16.toUInt8

/--
得到 `Int16`，它与 `UInt16` 二进制补码等价。
-/
def c093 := @UInt16.toInt16

/--
将16 位无符号整数转换为32 位无符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c094 := @UInt16.toUInt32

/--
将16 位无符号整数转换为64 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c095 := @UInt16.toUInt64

/--
将16 位无符号整数转换为平台字长无符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c096 := @UInt16.toUSize

/--
将32 位无符号整数转换为8 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c097 := @UInt32.toUInt8

/--
将32 位无符号整数转换为16 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c098 := @UInt32.toUInt16

/--
得到 `Int32`，它与 `UInt32` 二进制补码等价。
-/
def c099 := @UInt32.toInt32

/--
将32 位无符号整数转换为64 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c100 := @UInt32.toUInt64

/--
将32 位无符号整数转换为平台字长无符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c101 := @UInt32.toUSize

/--
将64 位无符号整数转换为8 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c102 := @UInt64.toUInt8

/--
将64 位无符号整数转换为16 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c103 := @UInt64.toUInt16

/--
将64 位无符号整数转换为32 位无符号整数。发生溢出时回绕。

此函数在运行时会被高效实现覆盖。
-/
def c104 := @UInt64.toUInt32

/--
得到 `Int64`，它与 `UInt64` 二进制补码等价。
-/
def c105 := @UInt64.toInt64

/--
将 64 位无符号整数转换为平台字长无符号整数。在 32 位机器上，此转换可能溢出并导致数值回绕（即对 `USize.size` 取模）。

此函数在运行时会被高效实现覆盖。
-/
def c106 := @UInt64.toUSize

/--
通过截断位向量表示，将平台字长有符号整数转换为8 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c107 := @ISize.toInt8

/--
通过截断位向量表示，将平台字长有符号整数转换为16 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c108 := @ISize.toInt16

/--
将平台字长有符号整数转换为 32 位有符号整数。

在 32 位平台上，此转换不会损失信息。在 64 位平台上，该整数的位向量表示会被截断为 32 位。此函数在运行时会被高效实现覆盖。

此函数在运行时会被高效实现覆盖。
-/
def c109 := @ISize.toInt32

/--
将平台字长有符号整数转换为表示同一数值的 64 位有符号整数。由于 `ISize` 为 `Int32` 或 `Int64`，此转换不会损失信息。

此函数在运行时会被高效实现覆盖。
-/
def c110 := @ISize.toInt64

/--
将8 位有符号整数转换为表示同一数值的16 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c111 := @Int8.toInt16

/--
将8 位有符号整数转换为表示同一数值的32 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c112 := @Int8.toInt32

/--
将8 位有符号整数转换为表示同一数值的64 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c113 := @Int8.toInt64

/--
将8 位有符号整数转换为表示同一数值的平台字长有符号整数。由于 `ISize` 为 `Int32` 或 `Int64`，此转换不会损失信息。

此函数在运行时会被高效实现覆盖。
-/
def c114 := @Int8.toISize

/--
通过截断位向量表示，将16 位有符号整数转换为8 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c115 := @Int16.toInt8

/--
将16 位有符号整数转换为表示同一数值的32 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c116 := @Int16.toInt32

/--
将16 位有符号整数转换为表示同一数值的64 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c117 := @Int16.toInt64

/--
将16 位有符号整数转换为表示同一数值的平台字长有符号整数。由于 `ISize` 为 `Int32` 或 `Int64`，此转换不会损失信息。

此函数在运行时会被高效实现覆盖。
-/
def c118 := @Int16.toISize

/--
通过截断位向量表示，将32 位有符号整数转换为8 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c119 := @Int32.toInt8

/--
通过截断位向量表示，将32 位有符号整数转换为16 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c120 := @Int32.toInt16

/--
将32 位有符号整数转换为表示同一数值的64 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c121 := @Int32.toInt64

/--
将32 位有符号整数转换为表示同一数值的平台字长有符号整数。由于 `ISize` 为 `Int32` 或 `Int64`，此转换不会损失信息。

此函数在运行时会被高效实现覆盖。
-/
def c122 := @Int32.toISize

/--
通过截断位向量表示，将64 位有符号整数转换为8 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c123 := @Int64.toInt8

/--
通过截断位向量表示，将64 位有符号整数转换为16 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c124 := @Int64.toInt16

/--
通过截断位向量表示，将64 位有符号整数转换为32 位有符号整数。

此函数在运行时会被高效实现覆盖。
-/
def c125 := @Int64.toInt32

/--
将 64 位有符号整数转换为平台字长有符号整数；在 32 位平台上会截断其位向量表示，在 64 位平台上则不会损失信息。

此函数在运行时会被高效实现覆盖。
-/
def c126 := @Int64.toISize

/--
得到 `Float`，其数值接近给定的 `ISize`。

如果给定 `ISize` 的数值存在对应的 `Float`，则返回精确值。如果不存在这样的 `Float`，则返回大于给定值的最小 `Float`，或小于给定值的最大 `Float`。

此函数具有基于 `Float.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c127 := @ISize.toFloat

/--
得到 `Float32`，其数值接近给定的 `ISize`。

如果给定 `ISize` 的数值存在对应的 `Float32`，则返回精确值。如果不存在这样的 `Float32`，则返回大于给定值的最小 `Float32`，或小于给定值的最大 `Float32`。

此函数具有基于 `Float32.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c128 := @ISize.toFloat32

/--
得到 `Float`，其数值与给定 `Int8` 相同。
-/
def c129 := @Int8.toFloat

/--
得到 `Float32`，其数值与给定 `Int8` 相同。
-/
def c130 := @Int8.toFloat32

/--
得到 `Float`，其数值与给定 `Int16` 相同。
-/
def c131 := @Int16.toFloat

/--
得到 `Float32`，其数值与给定 `Int16` 相同。
-/
def c132 := @Int16.toFloat32

/--
得到 `Float`，其数值与给定 `Int32` 相同。
-/
def c133 := @Int32.toFloat

/--
得到 `Float32`，其数值接近给定的 `Int32`。

如果给定 `Int32` 的数值存在对应的 `Float32`，则返回精确值。如果不存在这样的 `Float32`，则返回大于给定值的最小 `Float32`，或小于给定值的最大 `Float32`。

此函数具有基于 `Float32.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c134 := @Int32.toFloat32

/--
得到 `Float`，其数值接近给定的 `Int64`。

如果给定 `Int64` 的数值存在对应的 `Float`，则返回精确值。如果不存在这样的 `Float`，则返回大于给定值的最小 `Float`，或小于给定值的最大 `Float`。

此函数具有基于 `Float.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c135 := @Int64.toFloat

/--
得到 `Float32`，其数值接近给定的 `Int64`。

如果给定 `Int64` 的数值存在对应的 `Float32`，则返回精确值。如果不存在这样的 `Float32`，则返回大于给定值的最小 `Float32`，或小于给定值的最大 `Float32`。

此函数具有基于 `Float32.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c136 := @Int64.toFloat32

/--
得到 `Float`，其数值接近给定的 `USize`。

如果给定 `USize` 的数值存在对应的 `Float`，则返回精确值。如果不存在这样的 `Float`，则返回大于给定值的最小 `Float`，或小于给定值的最大 `Float`。

此函数具有基于 `Float.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c137 := @USize.toFloat

/--
得到 `Float32`，其数值接近给定的 `USize`。

如果给定 `USize` 的数值存在对应的 `Float32`，则返回精确值。如果不存在这样的 `Float32`，则返回大于给定值的最小 `Float32`，或小于给定值的最大 `Float32`。

此函数具有基于 `Float32.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c138 := @USize.toFloat32

/--
得到 `Float`，其数值与给定 `UInt8` 相同。
-/
def c139 := @UInt8.toFloat

/--
得到 `Float32`，其数值与给定 `UInt8` 相同。
-/
def c140 := @UInt8.toFloat32

/--
得到 `Float`，其数值与给定 `UInt16` 相同。
-/
def c141 := @UInt16.toFloat

/--
得到 `Float32`，其数值与给定 `UInt16` 相同。
-/
def c142 := @UInt16.toFloat32

/--
得到 `Float`，其数值与给定 `UInt32` 相同。
-/
def c143 := @UInt32.toFloat

/--
得到 `Float32`，其数值接近给定的 `UInt32`。

如果给定 `UInt32` 的数值存在对应的 `Float32`，则返回精确值。如果不存在这样的 `Float32`，则返回大于给定值的最小 `Float32`，或小于给定值的最大 `Float32`。

此函数具有基于 `Float32.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c144 := @UInt32.toFloat32

/--
得到 `Float`，其数值接近给定的 `UInt64`。

如果给定 `UInt64` 的数值存在对应的 `Float`，则返回精确值。如果不存在这样的 `Float`，则返回大于给定值的最小 `Float`，或小于给定值的最大 `Float`。

此函数具有基于 `Float.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c145 := @UInt64.toFloat

/--
得到 `Float32`，其数值接近给定的 `UInt64`。

如果给定 `UInt64` 的数值存在对应的 `Float32`，则返回精确值。如果不存在这样的 `Float32`，则返回大于给定值的最小 `Float32`，或小于给定值的最大 `Float32`。

此函数具有基于 `Float32.Model` 的逻辑模型，但在运行时会被高效实现覆盖。
-/
def c146 := @UInt64.toFloat32

/--
得到 `BitVec`，其中包含 `ISize` 的二进制补码表示。
-/
def c147 := @ISize.toBitVec

/--
得到 `ISize`，其二进制补码表示为给定的 `BitVec`。
-/
def c148 := @ISize.ofBitVec

/--
得到 `BitVec`，其中包含 `Int8` 的二进制补码表示。
-/
def c149 := @Int8.toBitVec

/--
得到 `Int8`，其二进制补码表示为给定的 `BitVec 8`。
-/
def c150 := @Int8.ofBitVec

/--
得到 `BitVec`，其中包含 `Int16` 的二进制补码表示。
-/
def c151 := @Int16.toBitVec

/--
得到 `Int16`，其二进制补码表示为给定的 `BitVec 16`。
-/
def c152 := @Int16.ofBitVec

/--
得到 `BitVec`，其中包含 `Int32` 的二进制补码表示。
-/
def c153 := @Int32.toBitVec

/--
得到 `Int32`，其二进制补码表示为给定的 `BitVec 32`。
-/
def c154 := @Int32.ofBitVec

/--
得到 `BitVec`，其中包含 `Int64` 的二进制补码表示。
-/
def c155 := @Int64.toBitVec

/--
得到 `Int64`，其二进制补码表示为给定的 `BitVec 64`。
-/
def c156 := @Int64.ofBitVec

/--
将 `USize` 转换为对应的 `Fin USize.size`。
-/
def c157 := @USize.toFin

/--
将 `UInt8` 转换为对应的 `Fin UInt8.size`。
-/
def c158 := @UInt8.toFin

/--
将 `UInt16` 转换为对应的 `Fin UInt16.size`。
-/
def c159 := @UInt16.toFin

/--
将 `UInt32` 转换为对应的 `Fin UInt32.size`。
-/
def c160 := @UInt32.toFin

/--
将 `UInt64` 转换为对应的 `Fin UInt64.size`。
-/
def c161 := @UInt64.toFin

/--
将 `Fin USize.size` 转换为对应的 `USize`。
-/
def c162 := @USize.ofFin

/--
将 `Fin UInt8.size` 转换为对应的 `UInt8`。
-/
def c163 := @UInt8.ofFin

/--
将 `Fin UInt16.size` 转换为对应的 `UInt16`。
-/
def c164 := @UInt16.ofFin

/--
将 `Fin UInt32.size` 转换为对应的 `UInt32`。
-/
def c165 := @UInt32.ofFin

/--
将 `Fin UInt64.size` 转换为对应的 `UInt64`。
-/
def c166 := @UInt64.ofFin

/--
将平台字长无符号整数转换为十进制字符串。

此函数在运行时会被高效实现覆盖。

示例：
* `USize.repr 0 = "0"`
* `USize.repr 28 = "28"`
* `USize.repr 307 = "307"`
-/
def c167 := @USize.repr

/--
如果 `UInt32` 小于 `0x110000`，并且不是代理代码点（从 `0xd800` 到 `0xdfff` 的闭区间），那么它表示有效的 Unicode 码点。
-/
def c168 := @UInt32.isValidChar

/--
平台字长无符号整数的按位与。通常通过 `&&&` 运算符访问。

仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c169 := @USize.land

/--
平台字长有符号整数的按位与。通常通过 `&&&` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c170 := @ISize.land

/--
8 位无符号整数的按位与。通常通过 `&&&` 运算符访问。

仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c171 := @UInt8.land

/--
8 位有符号整数的按位与。通常通过 `&&&` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c172 := @Int8.land

/--
16 位无符号整数的按位与。通常通过 `&&&` 运算符访问。

仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c173 := @UInt16.land

/--
16 位有符号整数的按位与。通常通过 `&&&` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c174 := @Int16.land

/--
32 位无符号整数的按位与。通常通过 `&&&` 运算符访问。

仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c175 := @UInt32.land

/--
32 位有符号整数的按位与。通常通过 `&&&` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c176 := @Int32.land

/--
64 位无符号整数的按位与。通常通过 `&&&` 运算符访问。

仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c177 := @UInt64.land

/--
64 位有符号整数的按位与。通常通过 `&&&` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位都为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c178 := @Int64.land

/--
平台字长无符号整数的按位或。通常通过 `|||` 运算符访问。

只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c179 := @USize.lor

/--
平台字长有符号整数的按位或。通常通过 `|||` 运算符访问。

按照二进制补码表示，只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c180 := @ISize.lor

/--
8 位无符号整数的按位或。通常通过 `|||` 运算符访问。

只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c181 := @UInt8.lor

/--
8 位有符号整数的按位或。通常通过 `|||` 运算符访问。

按照二进制补码表示，只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c182 := @Int8.lor

/--
16 位无符号整数的按位或。通常通过 `|||` 运算符访问。

只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c183 := @UInt16.lor

/--
16 位有符号整数的按位或。通常通过 `|||` 运算符访问。

按照二进制补码表示，只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c184 := @Int16.lor

/--
32 位无符号整数的按位或。通常通过 `|||` 运算符访问。

只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c185 := @UInt32.lor

/--
32 位有符号整数的按位或。通常通过 `|||` 运算符访问。

按照二进制补码表示，只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c186 := @Int32.lor

/--
64 位无符号整数的按位或。通常通过 `|||` 运算符访问。

只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c187 := @UInt64.lor

/--
64 位有符号整数的按位或。通常通过 `|||` 运算符访问。

按照二进制补码表示，只要输入整数的对应位中至少有一个为 1，结果整数的该位就为 1。

此函数在运行时会被高效实现覆盖。
-/
def c188 := @Int64.lor

/--
平台字长无符号整数的按位异或。通常通过 `^^^` 运算符访问。

仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c189 := @USize.xor

/--
平台字长有符号整数的按位异或。通常通过 `^^^` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c190 := @ISize.xor

/--
8 位无符号整数的按位异或。通常通过 `^^^` 运算符访问。

仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c191 := @UInt8.xor

/--
8 位有符号整数的按位异或。通常通过 `^^^` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c192 := @Int8.xor

/--
16 位无符号整数的按位异或。通常通过 `^^^` 运算符访问。

仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c193 := @UInt16.xor

/--
16 位有符号整数的按位异或。通常通过 `^^^` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c194 := @Int16.xor

/--
32 位无符号整数的按位异或。通常通过 `^^^` 运算符访问。

仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c195 := @UInt32.xor

/--
32 位有符号整数的按位异或。通常通过 `^^^` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c196 := @Int32.xor

/--
64 位无符号整数的按位异或。通常通过 `^^^` 运算符访问。

仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c197 := @UInt64.xor

/--
64 位有符号整数的按位异或。通常通过 `^^^` 运算符访问。

按照二进制补码表示，仅当两个输入整数的对应位中恰有一个为 1 时，结果整数的该位才为 1。

此函数在运行时会被高效实现覆盖。
-/
def c198 := @Int64.xor

/--
平台字长无符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。

此函数在运行时会被高效实现覆盖。
-/
def c199 := @USize.complement

/--
平台字长有符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。整数使用二进制补码表示，因此 `ISize.complement a = -(a + 1)`。

此函数在运行时会被高效实现覆盖。
-/
def c200 := @ISize.complement

/--
8 位无符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。

此函数在运行时会被高效实现覆盖。
-/
def c201 := @UInt8.complement

/--
8 位有符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。整数使用二进制补码表示，因此 `Int8.complement a = -(a + 1)`。

此函数在运行时会被高效实现覆盖。
-/
def c202 := @Int8.complement

/--
16 位无符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。

此函数在运行时会被高效实现覆盖。
-/
def c203 := @UInt16.complement

/--
16 位有符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。整数使用二进制补码表示，因此 `Int16.complement a = -(a + 1)`。

此函数在运行时会被高效实现覆盖。
-/
def c204 := @Int16.complement

/--
32 位无符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。

此函数在运行时会被高效实现覆盖。
-/
def c205 := @UInt32.complement

/--
32 位有符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。整数使用二进制补码表示，因此 `Int32.complement a = -(a + 1)`。

此函数在运行时会被高效实现覆盖。
-/
def c206 := @Int32.complement

/--
64 位无符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。

此函数在运行时会被高效实现覆盖。
-/
def c207 := @UInt64.complement

/--
64 位有符号整数的按位补码（也称按位取反）。通常通过 `~~~` 运算符访问。

结果整数的每一位都与输入整数的对应位相反。整数使用二进制补码表示，因此 `Int64.complement a = -(a + 1)`。

此函数在运行时会被高效实现覆盖。
-/
def c208 := @Int64.complement

/--
平台字长无符号整数的按位左移。通常通过 `<<<` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c209 := @USize.shiftLeft

/--
平台字长有符号整数的按位左移。通常通过 `<<<` 运算符访问。

有符号整数按照二进制补码表示解释为位向量。

此函数在运行时会被高效实现覆盖。
-/
def c210 := @ISize.shiftLeft

/--
8 位无符号整数的按位左移。通常通过 `<<<` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c211 := @UInt8.shiftLeft

/--
8 位有符号整数的按位左移。通常通过 `<<<` 运算符访问。

有符号整数按照二进制补码表示解释为位向量。

此函数在运行时会被高效实现覆盖。
-/
def c212 := @Int8.shiftLeft

/--
16 位无符号整数的按位左移。通常通过 `<<<` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c213 := @UInt16.shiftLeft

/--
16 位有符号整数的按位左移。通常通过 `<<<` 运算符访问。

有符号整数按照二进制补码表示解释为位向量。

此函数在运行时会被高效实现覆盖。
-/
def c214 := @Int16.shiftLeft

/--
32 位无符号整数的按位左移。通常通过 `<<<` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c215 := @UInt32.shiftLeft

/--
32 位有符号整数的按位左移。通常通过 `<<<` 运算符访问。

有符号整数按照二进制补码表示解释为位向量。

此函数在运行时会被高效实现覆盖。
-/
def c216 := @Int32.shiftLeft

/--
64 位无符号整数的按位左移。通常通过 `<<<` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c217 := @UInt64.shiftLeft

/--
64 位有符号整数的按位左移。通常通过 `<<<` 运算符访问。

有符号整数按照二进制补码表示解释为位向量。

此函数在运行时会被高效实现覆盖。
-/
def c218 := @Int64.shiftLeft

/--
平台字长无符号整数的按位右移。通常通过 `>>>` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c219 := @USize.shiftRight

/--
平台字长有符号整数的算术右移。通常通过 `<<<` 运算符访问。

高位用最高有效位的值填充。

此函数在运行时会被高效实现覆盖。
-/
def c220 := @ISize.shiftRight

/--
8 位无符号整数的按位右移。通常通过 `>>>` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c221 := @UInt8.shiftRight

/--
8 位有符号整数的算术右移。通常通过 `<<<` 运算符访问。

高位用最高有效位的值填充。

此函数在运行时会被高效实现覆盖。
-/
def c222 := @Int8.shiftRight

/--
16 位无符号整数的按位右移。通常通过 `>>>` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c223 := @UInt16.shiftRight

/--
16 位有符号整数的算术右移。通常通过 `<<<` 运算符访问。

高位用最高有效位的值填充。

此函数在运行时会被高效实现覆盖。
-/
def c224 := @Int16.shiftRight

/--
32 位无符号整数的按位右移。通常通过 `>>>` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c225 := @UInt32.shiftRight

/--
32 位有符号整数的算术右移。通常通过 `<<<` 运算符访问。

高位用最高有效位的值填充。

此函数在运行时会被高效实现覆盖。
-/
def c226 := @Int32.shiftRight

/--
64 位无符号整数的按位右移。通常通过 `>>>` 运算符访问。

此函数在运行时会被高效实现覆盖。
-/
def c227 := @UInt64.shiftRight

/--
64 位有符号整数的算术右移。通常通过 `<<<` 运算符访问。

高位用最高有效位的值填充。

此函数在运行时会被高效实现覆盖。
-/
def c228 := @Int64.shiftRight

/--
只有一个元素的规范类型。该元素写作 `()`。

`Unit` 有多种用途：
* 可用于表示从函数调用返回、但不提供其他信息的控制流。
* 返回 `Unit` 的单子操作只产生副作用而不计算值。
* 在多态类型中，可用它表示某个字段不存储数据。
-/
def c229 := @Unit

/--
单元类型的唯一元素。

它可以写成空元组：`()`。
-/
def c230 := @Unit.unit

/--
只有一个元素的规范宇宙多态类型。

当上下文要求类型是宇宙多态的、因而不能使用 `Unit` 时，应使用此类型。
-/
inductive c231.{u} : Type u where
  /--
  宇宙多态单元类型的唯一元素。
  -/
  | unit

/--
布尔值 `true` 和 `false`。

从逻辑上说，它等价于 `Prop`（命题的类型）。这一区别对编程非常重要：命题及其证明都会被代码生成器擦除，而 `Bool` 对应大多数编程语言中的布尔类型，恰好携带一位运行时信息。
-/
inductive c232 where
  /--
  布尔值 `false`，不要与命题 `False` 混淆。
  -/
  | false
  /--
  布尔值 `true`，不要与命题 `True` 混淆。
  -/
  | true

/--
条件函数。

`cond c x y` 与 `if c then x else y` 相同，但针对布尔条件而非可判定命题进行了优化。也可用记法 `bif c then x else y` 书写。

与 `ite` 一样，`cond` 被声明为 `@[macro_inline]`，这会展开 `cond` 的应用。因此，运行时在选定 `x` 或 `y` 之前不会求值它们，并且只会求值选中的分支。
-/
def c233 := @cond

/--
依赖条件函数，其中每个分支都会获得一个关于条件值的局部假设。这样既可将该值用于证明，也可用于控制流。

`dcond c (fun h => x) (fun h => y)` 与 `if h : c then x else y` 相同，但针对布尔条件而非可判定命题进行了优化。与非依赖版本 `cond` 不同，`dcond` 没有专用记法。

与 `ite`、`dite` 和 `cond` 一样，`dcond` 被声明为 `@[macro_inline]`，这会展开 `dcond` 的应用。因此，运行时在选定 `x` 或 `y` 之一之前不会求值它们，并且只会求值选中的分支。`dcond` 旨在用于元编程，而非已验证程序，因此不提供行为引理。
-/
def c234 := @Bool.dcond

/--
布尔否定，也称布尔补。`not x` 可写作 `!x`。

此函数将值 `true` 映射为 `false`，并将值 `false` 映射为 `true`。对应的命题联结词是 `Not : Prop → Prop`。

标识符中记法的约定：

 * 在标识符中，`!` 的推荐拼写是 `not`。
-/
def c235 := @Bool.not

/--
布尔“与”，也称合取。`and x y` 可写作 `x && y`。

对应的命题联结词是 `And : Prop → Prop → Prop`，用 `∧` 运算符书写。

布尔 `and` 是 `@[macro_inline]` 函数，从而实现短路求值：若 `x` 为 `false`，则运行时不会求值 `y`。

标识符中记法的约定：

 * 在标识符中，`&&` 的推荐拼写是 `and`。

 * 在标识符中，`||` 的推荐拼写是 `or`。
-/
def c236 := @Bool.and

/--
布尔“或”，也称析取。`or x y` 可写作 `x || y`。

对应的命题联结词是 `Or : Prop → Prop → Prop`，用 `∨` 运算符书写。

布尔 `or` 是 `@[macro_inline]` 函数，从而实现短路求值：若 `x` 为 `true`，则运行时不会求值 `y`。
-/
def c237 := @Bool.or

/--
布尔“异或”。`xor x y` 可写作 `x ^^ y`。

`x ^^ y` 为 `true`，恰当且仅当 `x` 和 `y` 中恰有一个为 `true`。与 `and` 和 `or` 不同，它不具有短路行为，因为任一参数的值都不能单独确定最终结果。此外，与 `and` 和 `or` 不同，它没有常用的对应命题联结词。

示例：
 * `false ^^ false = false`
 * `true ^^ false = true`
 * `false ^^ true = true`
 * `true ^^ true = false`

标识符中记法的约定：

 * 在标识符中，`^^` 的推荐拼写是 `xor`。
-/
def c238 := @Bool.xor

/--
判定两个布尔值是否相等。

通常应通过它所支持的 `DecidableEq Bool` 实例调用此函数。
-/
def c239 := @Bool.decEq

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c240 := @Bool.toISize

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c241 := @Bool.toUInt8

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c242 := @Bool.toUInt16

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c243 := @Bool.toUInt32

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c244 := @Bool.toUInt64

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c245 := @Bool.toUSize

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c246 := @Bool.toInt8

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c247 := @Bool.toInt16

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c248 := @Bool.toInt32

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c249 := @Bool.toInt64

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c250 := @Bool.toNat

/--
将 `true` 转换为 `1`，将 `false` 转换为 `0`。
-/
def c251 := @Bool.toInt

end Manual.ZhDocString.Ch19Ch20.G7
