window.docContents[90].resolve({"/Basic-Types/Natural-Numbers/#nat-api-iteration":{"contents":"许多迭代运算符有两个版本：结构递归版本和尾递归版本。\n结构递归版本通常在定义等价重要的上下文中更容易使用，因为当只知道自然数的某些前缀时它就可以进行计算。\n\n将函数对初始值应用指定次数。换言之，迭代 f 共 n 次，作用于 a。示例：* Nat.repeat f 3 a = f <| f <| f <| a* Nat.repeat (· ++ \"!\") 4 \"Hello\" = \"Hello!!!!\"\n\n将函数对初始值应用指定次数。换言之，迭代 f 共 n 次，作用于 a。这是 Nat.repeat 的尾递归版本，供运行时使用。示例：* Nat.repeatTR f 3 a = f <| f <| f <| a* Nat.repeatTR (· ++ \"!\") 4 \"Hello\" = \"Hello!!!!\"\n\n迭代应用函数 f：从初始值 init 开始，共执行 n 次；每一步按递增顺序，把 f 应用于当前值以及下一个小于 n 的自然数。示例：* Nat.fold 3 f init = (init |> f 0 (by simp) |> f 1 (by simp) |> f 2 (by simp))* Nat.fold 4 (fun i _ xs => xs.push i) #[] = #[0, 1, 2, 3]* Nat.fold 0 (fun i _ xs => xs.push i) #[] = #[]\n\n迭代应用函数 f：从初始值 init 开始，共执行 n 次；每一步按递增顺序，把 f 应用于当前值以及下一个小于 n 的自然数。这是 Nat.fold 的尾递归版本，供运行时使用。示例：* Nat.foldTR 3 f init = (init |> f 0 (by simp) |> f 1 (by simp) |> f 2 (by simp))* Nat.foldTR 4 (fun i _ xs => xs.push i) #[] = #[0, 1, 2, 3]* Nat.foldTR 0 (fun i _ xs => xs.push i) #[] = #[]\n\n迭代应用单子函数 f：从初始值 init 开始，共执行 n 次；每一步按递增顺序，把 f 应用于当前值以及下一个小于 n 的自然数。\n\n迭代应用函数 f：从初始值 init 开始，共执行 n 次；每一步按递减顺序，把 f 应用于当前值以及下一个小于 n 的自然数。示例：* Nat.foldRev 3 f init = (f 0 (by simp) <| f 1 (by simp) <| f 2 (by simp) init)* Nat.foldRev 4 (fun i _ xs => xs.push i) #[] = #[3, 2, 1, 0]* Nat.foldRev 0 (fun i _ xs => xs.push i) #[] = #[]\n\n迭代应用单子函数 f：从初始值 init 开始，共执行 n 次；每一步按递减顺序，把 f 应用于当前值以及下一个小于 n 的自然数。\n\n按递增顺序，对所有小于某个界的数执行单子动作。示例：#eval Nat.forM 5 fun i _ => IO.println i\n0\n1\n2\n3\n4\n\n\n按递减顺序，对所有小于某个界的数执行单子动作。示例：#eval Nat.forRevM 5 fun i _ => IO.println i\n4\n3\n2\n1\n0\n\n\n检查对每个严格小于给定界的数，f 是否都返回 true。示例：* Nat.all 4 (fun i _ => i < 5) = true* Nat.all 7 (fun i _ => i < 5) = false* Nat.all 7 (fun i _ => i % 2 = 0) = false* Nat.all 1 (fun i _ => i % 2 = 0) = true\n\n检查对每个严格小于给定界的数，f 是否都返回 true。这是与 Nat.all 等价的尾递归版本，供运行时使用。示例：* Nat.allTR 4 (fun i _ => i < 5) = true* Nat.allTR 7 (fun i _ => i < 5) = false* Nat.allTR 7 (fun i _ => i % 2 = 0) = false* Nat.allTR 1 (fun i _ => i % 2 = 0) = true\n\n检查是否存在某个小于给定界的数，使 f 返回 true。示例：* Nat.any 4 (fun i _ => i < 5) = true* Nat.any 7 (fun i _ => i < 5) = true* Nat.any 7 (fun i _ => i % 2 = 0) = true* Nat.any 1 (fun i _ => i % 2 = 1) = false\n\n检查是否存在某个小于给定界的数，使 f 返回 true。这是与 Nat.any 等价的尾递归版本，供运行时使用。示例：* Nat.anyTR 4 (fun i _ => i < 5) = true* Nat.anyTR 7 (fun i _ => i < 5) = true* Nat.anyTR 7 (fun i _ => i % 2 = 0) = true* Nat.anyTR 1 (fun i _ => i % 2 = 1) = false\n\n检查单子谓词 p 是否对所有小于给定界的数都返回 true。按递增顺序检查，p 一旦返回 false，便不再检查后续数字。\n\n检查是否存在某个小于给定界的数，使单子谓词 p 返回 true。按递增顺序检查，p 一旦返回 true，便不再检查后续数字。\n\n","context":"Lean 中文参考手册\u0009基本类型\u0009自然数\u0009API 参考","header":"20.1.4.6. 迭代","id":"/Basic-Types/Natural-Numbers/#nat-api-iteration"},"/Definitions/Recursive-Definitions/#well-founded-recursion":{"contents":"以 良基递归 定义的函数，是指其中每次递归调用的实参都在某种适当意义下比函数形参更小的函数。\n与结构递归不同，后者要求递归定义满足特定的句法要求，而良基递归的定义使用的是语义论证。\n这使得更大一类递归定义能够被接受。\n此外，当 Lean 的自动化无法构造终止性证明时，也可以手工给出。\n\nLean 编译器会以完全相同的方式对待所有这些定义。\n在 Lean 的逻辑中，使用良基递归的定义通常不会 在定义上 归约。\n不过，这些归约在命题相等层面仍然成立，而 Lean 会自动证明它们。\n这通常不会让证明良基递归定义的性质变得更困难，因为可以利用这些命题性的归约来推理函数行为。\n但这也意味着，这类函数通常不太适合出现在类型中。\n即便其归约行为碰巧在定义上成立，它在内核中的速度通常仍比结构递归定义慢得多，因为内核必须连同定义一起展开终止性证明。\n因此，只要可能，那些打算在类型中使用、或在其他依赖定义相等的重要场景中使用的递归函数，都应优先定义为结构递归。\n\n若要显式使用良基递归，可以在函数或定理定义上添加 termination_by 子句，用来指定函数终止所依据的 度量。\n该度量应是一个在每次递归调用时都会减小的项；它可以是函数的某个形参、若干形参组成的元组，也可以是任意其他项。\n这个度量的类型必须配备一个 良基关系，它决定了“度量减小”究竟意味着什么。\n\n显式良基递归termination_by 子句用来引入终止性论证。可选 => 之前的标识符可以把尚未在声明头中绑定的函数形参带入作用域，而后面必需的项必须指明函数的某个形参，无论它是在声明头中引入，还是在该子句中局部引入。\n\n通过反复减法定义除法除法可以刻画为“除数能从被除数中减去多少次”。\n这个操作不能用结构递归来精译，因为减法不是模式匹配。\n不过 n 的值确实会在每次递归调用时减小，因此可以用良基递归来为这种“反复减法求除法”的定义提供正当性。def div (n k : Nat) : Nat :=\n  if k = 0 then 0\n  else if k > n then 0\n  else 1 + div (n - k) k\ntermination_by n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n","context":"Lean 中文参考手册\u0009定义\u0009递归定义","header":"7.6.3. 良基递归","id":"/Definitions/Recursive-Definitions/#well-founded-recursion"},"/Run-Time-Code/Reference-Counting/#Lean-__________________--_______________--____________--________________________IR___":{"contents":"编译器选项 trace.compiler.ir.result 可用于查看函数的编译器中间表示（IR）。\n在这种中间表示中，引用计数、内存分配和复用都是显式的：\n\n* isShared 运算符检查引用计数是否为 1。* ctor_n 分配某个类型的第 n 个构造器。* proj_n 从构造器值中取出第 n 个字段。* set x﻿[n﻿] 修改 x 中构造器的第 n 个字段。* ret x 返回 x 中的值。\n\n引用计数操作的具体方式可能取决于内联等优化阶段的结果。\n绝大多数 Lean 代码无须关注这些细节就能获得良好性能，但在编写性能关键型代码时，掌握如何诊断唯一引用相关的问题可能非常重要。\n\n启用后，跟踪 Lean 编译器中间表示（IR）生成阶段的最终结果。\n\nIR 中的引用计数通过编译器中间表示（IR）可以观察引用计数何时递增，这有助于诊断以下情形：本以为某个值只有一个传入引用，但它实际上却被共享。\n这里，process 和 process' 都接受一个字符串参数，使用 String.Pos.set 修改它，并返回一对字符串。\nprocess 将常量字符串作为二元组的第二个元素返回，而 process' 则返回原字符串。set_option trace.compiler.ir.result true\ndef process (str : String) : String × String :=\n  (String.Pos.Raw.set str 0 ' ', \"\")\ndef process' (str : String) : String × String:=\n  (String.Pos.Raw.set str 0 ' ', str)\nprocess 的 IR 中不包含 inc 或 dec 指令。\n如果传入的字符串 x_1 是唯一引用，那么将它传给 String.Pos.set 时，它仍然是唯一引用，因此可以就地修改：[Compiler.IR] [result]\n    def process._closed_0 : obj :=\n      let x_1 : obj := \"\";\n      ret x_1\n    def process (x_1 : obj) : obj :=\n      let x_2 : tagged := 0;\n      let x_3 : u32 := 32;\n      let x_4 : obj := String.Pos.Raw.set x_1 x_2 x_3;\n      let x_5 : obj := process._closed_0;\n      let x_6 : obj := ctor_0[Prod.mk] x_4 x_5;\n      ret x_6\n另一方面，process' 的 IR 会在调用 String.Pos.set 之前递增该字符串的引用计数。\n因此，无论 x_1 的原始引用是否唯一，修改后的字符串 x_4 都是一个副本：[Compiler.IR] [result]\n    def process' (x_1 : obj) : obj :=\n      let x_2 : tagged := 0;\n      let x_3 : u32 := 32;\n      inc x_1;\n      let x_4 : obj := String.Pos.Raw.set x_1 x_2 x_3;\n      let x_5 : obj := ctor_0[Prod.mk] x_4 x_1;\n      ret x_5\n\n\nIR 中的内存复用函数 discardElems 是 List.map 的简化版本，它将列表中的每个元素替换为 ()。\n查看其中间表示可以看出，当列表的引用唯一时，它会复用列表的内存。set_option trace.compiler.ir.result true\n\ndef discardElems : List α → List Unit\n  | [] => []\n  | _ :: xs => () :: discardElems xs\n这会生成如下 IR：[Compiler.IR] [result]\n    def discardElems._redArg (x_1 : tobj) : tobj :=\n      case x_1 : tobj of\n      List.nil →\n        let x_2 : tagged := ctor_0[List.nil];\n        ret x_2\n      List.cons →\n        let x_3 : tobj := proj[1] x_1;\n        block_4 (x_5 : tobj) (x_6 : u8) :=\n          let x_7 : tagged := ctor_0[PUnit.unit];\n          let x_8 : tobj := discardElems._redArg x_3;\n          block_9 (x_10 : obj) :=\n            ret x_10;\n          case x_6 : u8 of\n          Bool.false →\n            set x_5[1] := x_8;\n            set x_5[0] := x_7;\n            jmp block_9 x_5\n          Bool.true →\n            let x_11 : obj := ctor_1[List.cons] x_7 x_8;\n            jmp block_9 x_11;\n        let x_12 : u8 := isShared x_1;\n        case x_12 : u8 of\n        Bool.false →\n          let x_13 : tobj := proj[0] x_1;\n          dec x_13;\n          jmp block_4 x_1 x_12\n        Bool.true →\n          inc x_3;\n          dec x_1;\n          jmp block_4 ◾ x_12\n[Compiler.IR] [result]\n    def discardElems (x_1 : ◾) (x_2 : tobj) : tobj :=\n      let x_3 : tobj := discardElems._redArg x_2;\n      ret x_3\n在 IR 中，List.cons 分支会显式检查参数值是否被共享（即其引用计数是否大于一）。\n如果引用唯一，则会递减被丢弃的列表元素 x_5 的引用计数，并复用构造器值。\n如果引用被共享，则会在 x_11 中为结果分配一个新的 List.cons。\n\n","context":"Lean 中文参考手册\u0009运行时代码\u0009引用计数","header":"12.2.2. 编译器中间表示（IR）","id":"/Run-Time-Code/Reference-Counting/#Lean-__________________--_______________--____________--________________________IR___"},"/The--mvcgen--tactic/Predicate-Transformers/#mvcgen-notation":{"contents":"有状态谓词的语法与普通 Lean 项的语法有所重叠。\n特别是，有状态谓词使用逻辑联结词和量词的通常语法。\n在前置条件和后置条件等明显需要有状态谓词的上下文中，相关语法会自动启用；其他上下文必须使用 spred 显式启用该语法。\n使用 term 运算符可恢复这些运算符的通常含义。\n\n谓词项spred 表示应将逻辑联结词和量词理解为有状态谓词中的对应构造，而 term 表示它们应取通常含义。\n\n","context":"Lean 中文参考手册\u0009mvcgen  策略\u0009谓词变换器概述\u0009有状态谓词","header":"17.2.1.2. 记法","id":"/The--mvcgen--tactic/Predicate-Transformers/#mvcgen-notation"},"/releases/v4.0.0-m5/#release-v4___0___0-m5":{"contents":"这是 Lean 4 的第五个里程碑版本。它包含许多改进和许多新特性。\n自上一个里程碑以来，我们已有 1495 次提交。\n\n贡献者：\n\n   885  Leonardo de Moura\n   310  Sebastian Ullrich\n    69  E.W.Ayers\n    66  Wojciech Nawrocki\n    49  Gabriel Ebner\n    38  Mario Carneiro\n    22  larsk21\n    10  tydeu\n     6  Ed Ayers\n     6  Mariana Alanis\n     4  Chris Lovett\n     3  Jannis Limperg\n     2  François G. Dorais\n     2  Henrik Böving\n     2  Jakob von Raumer\n     2  Scott Morrison\n     2  Siddharth\n     1  Andrés Goens\n     1  Arthur Paulino\n     1  Connor Baker\n     1  Joscha\n     1  KaseQuark\n     1  Lars\n     1  Mac\n     1  Marcus Rossel\n     1  Patrick Massot\n     1  Siddharth Bhat\n     1  Timo\n     1  Vincent de Haan\n     1  William Blake\n     1  Yuri de Wit\n     1  ammkrn\n     1  asdasd1dsadsa\n     1  kzvi\n\n\n* 将 Lake 更新到 v4.0.0。详细变更请参见 v4.0.0 发布说明。* 现在支持位于不同命名空间中的互递归声明。例如：mutual\n  def Foo.boo (x : Nat) :=\n    match x with\n    | 0 => 1\n    | x + 1 => 2*Boo.bla x\n\n  def Boo.bla (x : Nat) :=\n    match x with\n    | 0 => 2\n    | x+1 => 3*Foo.boo x\nend\n系统会为公共前缀自动创建一个 namespace。例如：mutual\n  def Tst.Foo.boo (x : Nat) := ...\n  def Tst.Boo.bla (x : Nat) := ...\nend\n会展开为namespace Tst\nmutual\n  def Foo.boo (x : Nat) := ...\n  def Boo.bla (x : Nat) := ...\nend\nend Tst\n* 允许用户为现有类型类安装自己的 deriving 处理器。\n示例见 Simple.lean。* 添加策略 congr (num)?。更多细节见文档字符串。* 缺失文档检查器* match 语法记法现在会检查未使用的分支。参见 issue #1371。* 为结构体实例字段提供自动补全。例如：example : Nat × Nat := {\n  f -- HERE\n}\nfst 现在会出现在自动补全建议列表中。* 为点式标识符记法提供自动补全。例如：example : Nat :=\n  .su -- HERE\nsucc 现在会出现在自动补全建议列表中。* 声明 OfNat 实例时不再需要 nat_lit。参见 issues #1389 和 #875。例如：inductive Bit where\n  | zero\n  | one\n\ninstance inst0 : OfNat Bit 0 where\n  ofNat := Bit.zero\n\ninstance : OfNat Bit 1 where\n  ofNat := Bit.one\n\nexample : Bit := 0\nexample : Bit := 1\n* 添加 [elabAsElim] 属性（在 Lean 3 中名为 elab_as_eliminator）。动机：简化 Mathlib 向 Lean 4 的移植。* Trans 类型类现在接受位于 Type u 中的关系。参见这条 Zulip 讨论。* 接受未经转义的关键字作为归纳类型构造子名称。在使用处通常可以通过点记法避免转义。inductive MyExpr\n  | let : ...\n\ndef f : MyExpr → MyExpr\n  | .let ... => .let ...\n* 对形如 [Nat -> Decidable p] 的参数化局部实例报错。类型类解析过程\n无法使用这类局部实例，因为该参数不具有前向依赖。\n可使用 set_option checkBinderAnnotations false 关闭此检查。* 添加选项 pp.showLetValues。当其设为 false 时，信息视图会隐藏目标中 let 变量的值。\n默认情况下，在显示策略目标时它为 true，否则为 false。\n更多细节见 问题 #1345。* 添加选项 warningAsError。设为 true 时，警告消息会被当作错误处理。* 在模式中支持点记法与具名参数。例如：def getForallBinderType (e : Expr) : Expr :=\n  match e with\n  | .forallE (binderType := type) .. => type\n  | _ => panic! \"forall expected\"\n* “跳转到定义”现在可用于以下属性中嵌入的函数名：\n@[implementedBy funName]、@[tactic parserName]、@[termElab parserName]、@[commandElab parserName]、\n@[builtinTactic parserName]、@[builtinTermElab parserName] 和 @[builtinCommandElab parserName]。\n参见 问题 #1350。* 提升 MVarId 方法的可发现性。参见 问题 #1346。\n我们仍需为 FVarId、LVarId、Expr 以及其他对象添加类似的方法。\n许多现有方法已被标记为弃用。* 添加属性 [deprecated]，用于标记已弃用的声明。例如：def g (x : Nat) := x + 1\n\n-- Whenever `f` is used, a warning message is generated suggesting to use `g` instead.\n@[deprecated g]\ndef f (x : Nat) := x + 1\n\n#check f 0 -- warning: `f` has been deprecated, use `g` instead\n\n-- Whenever `h` is used, a warning message is generated.\n@[deprecated]\ndef h (x : Nat) := x + 1\n\n#check h 0 -- warning: `h` has been deprecated\n* 为 universe level 元变量 id 添加类型 LevelMVarId（及缩写 LMVarId）。\n动机：防止元编程者混淆 universe 元变量 id 与表达式元变量 id。* 改进 calc 项与策略。参见 问题 #1342。* 放宽 antiquotation 解析，进一步减少了显式 $x:p antiquotation 种类注解的需求。* 为归纳类型中的计算字段添加支持。例如：inductive Exp\n  | var (i : Nat)\n  | app (a b : Exp)\nwith\n  @[computedField] hash : Exp → Nat\n    | .var i => i\n    | .app a b => a.hash * b.hash + 1\n随后，Exp.hash 函数的结果会作为额外的“计算”字段存储在 .var 和 .app 构造子中；\nExp.hash 直接访问该字段，因此可在常数时间内运行（即使面对 DAG 风格的值也是如此）。* 更新 a[i] 记法。它现在基于如下类型类class GetElem (cont : Type u) (idx : Type v) (elem : outParam (Type w)) (dom : outParam (cont → idx → Prop)) where\n  getElem (xs : cont) (i : idx) (h : dom xs i) : Elem\n记法 a[i] 现在定义如下macro:max x:term noWs \"[\" i:term \"]\" : term => `(getElem $x $i (by get_elem_tactic))\ni 是合法索引的证明由策略 get_elem_tactic 自动合成。\n例如，类型 Array α 具有如下实例instance : GetElem (Array α) Nat α fun xs i => LT.lt i xs.size where ...\ninstance : GetElem (Array α) USize α fun xs i => LT.lt i.toNat xs.size where ...\n你可以使用记法 a[i]'h 手动给出该证明。\n此外还引入了另外两种记法：a[i]! 与 a[i]?。对于 a[i]!，若 i 不是合法索引，则会在\n运行时产生 panic 报错消息。a[i]? 的类型是 Option α，如果索引 i 非法，\na[i]? 会求值为 none。\n这三种新记法定义如下：@[inline] def getElem' [GetElem cont idx elem dom] (xs : cont) (i : idx) (h : dom xs i) : elem :=\ngetElem xs i h\n\n@[inline] def getElem! [GetElem cont idx elem dom] [Inhabited elem] (xs : cont) (i : idx) [Decidable (dom xs i)] : elem :=\n  if h : _ then getElem xs i h else panic! \"index out of bounds\"\n\n@[inline] def getElem? [GetElem cont idx elem dom] (xs : cont) (i : idx) [Decidable (dom xs i)] : Option elem :=\n  if h : _ then some (getElem xs i h) else none\n\nmacro:max x:term noWs \"[\" i:term \"]\" noWs \"?\" : term => `(getElem? $x $i)\nmacro:max x:term noWs \"[\" i:term \"]\" noWs \"!\" : term => `(getElem! $x $i)\nmacro x:term noWs \"[\" i:term \"]'\" h:term:max : term => `(getElem' $x $i $h)\n参见 Zulip 上的讨论。\n示例：example (a : Array Int) (i : Nat) : Int :=\n  a[i] -- Error: failed to prove index is valid ...\n\nexample (a : Array Int) (i : Nat) (h : i < a.size) : Int :=\n  a[i] -- Ok\n\nexample (a : Array Int) (i : Nat) : Int :=\n  a[i]! -- Ok\n\nexample (a : Array Int) (i : Nat) : Option Int :=\n  a[i]? -- Ok\n\nexample (a : Array Int) (h : a.size = 2) : Int :=\n  a[0]'(by rw [h]; decide) -- Ok\n\nexample (a : Array Int) (h : a.size = 2) : Int :=\n  have : 0 < a.size := by rw [h]; decide\n  have : 1 < a.size := by rw [h]; decide\n  a[0] + a[1] -- Ok\n\nexample (a : Array Int) (i : USize) (h : i.toNat < a.size) : Int :=\n  a[i] -- Ok\nget_elem_tactic 定义如下macro \"get_elem_tactic\" : tactic =>\n  `(first\n    | get_elem_tactic_trivial\n    | fail \"failed to prove index is valid, ...\"\n   )\n辅助策略 get_elem_tactic_trivial 可以通过 macro_rules 扩展。默认情况下，它会尝试 trivial、simp_arith，以及针对 Fin 的特殊情形。未来它还会尝试 linarith。\n你可以像下面这样用 my_tactic 扩展 get_elem_tactic_trivialmacro_rules\n| `(tactic| get_elem_tactic_trivial) => `(tactic| my_tactic)\n请注意，GetElem 中 Idx 的类型并不依赖于 Cont。因此，你不能写出这样的实例 instance : GetElem (Array α) (Fin ??) α fun xs i => ...，不过 Lean 库提供了如下辅助实例：instance [GetElem cont Nat elem dom] : GetElem cont (Fin n) elem fun xs i => dom xs i where\n  getElem xs i h := getElem xs i.1 h\n以及辅助策略macro_rules\n| `(tactic| get_elem_tactic_trivial) => `(tactic| apply Fin.val_lt_of_le; get_elem_tactic_trivial; done)\n例如：example (a : Array Nat) (i : Fin a.size) :=\n  a[i] -- Ok\n\nexample (a : Array Nat) (h : n ≤ a.size) (i : Fin n) :=\n  a[i] -- Ok\n* 更好地支持递归声明中的限定名。现在支持如下写法：namespace Nat\n  def fact : Nat → Nat\n  | 0 => 1\n  | n+1 => (n+1) * Nat.fact n\nend Nat\n* 在 #eval 中添加对 CommandElabM monad 的支持。例如：import Lean\n\nopen Lean Elab Command\n\n#eval do\n  let id := mkIdent `foo\n  elabCommand (← `(def $id := 10))\n\n#eval foo -- 10\n* 即使期望类型不可用，也会尝试精译 do 记法。当期望类型不可用时，我们仍然会延后精译。\n这一变更在编写如下示例时尤其有用#eval do\n  IO.println \"hello\"\n  IO.println \"world\"\n也就是说，我们不再需要使用 #eval show IO _ from do ... 这种写法。\n需要注意的是，当期望类型不可用时，自动单子提升的效果会变差。\n单子多态函数（例如 ST.Ref.get）同样需要期望类型。* 在 Linux 上，panic 现在默认会打印回溯；可通过将环境变量 LEAN_BACKTRACE 设为 0 来禁用。\n其他平台尚待确定。* 现在会在需要时自动引入 group(·) syntax 组合子，例如在 (...)+ 中使用多个解析器时。* 添加“类型化宏”：syntax antiquotation 产生和接受的语法树现在会记住其语法种类，从而避免意外生成格式错误的语法树，并减少对显式 :kind antiquotation 注解的需求。详情见该 PR。* 受保护定义的别名现在也同样受保护。例如：protected def Nat.double (x : Nat) := 2*x\n\nnamespace Ex\nexport Nat (double) -- Add alias Ex.double for Nat.double\nend Ex\n\nopen Ex\n#check Ex.double -- Ok\n#check double -- Error, `Ex.double` is alias for `Nat.double` which is protected\n* 使用 IO.getRandomBytes 为 IO.rand 初始化随机种子。参见该 PR中的讨论。* 改进点记法与别名之间的交互。更多细节参见 Zulip 上的讨论。\n例如：def Set (α : Type) := α → Prop\ndef Set.union (s₁ s₂ : Set α) : Set α := fun a => s₁ a ∨ s₂ a\ndef FinSet (n : Nat) := Fin n → Prop\n\nnamespace FinSet\n  export Set (union) -- FinSet.union is now an alias for `Set.union`\nend FinSet\n\nexample (x y : FinSet 10) : FinSet 10 :=\n  x.union y -- Works\n* ext 与 enter 这两个 conv 策略现在可以进入 let 声明内部。例如：example (g : Nat → Nat) (y : Nat) (h : let x := y + 1; g (0+x) = x) : g (y + 1) = y + 1 := by\n  conv at h => enter [x, 1, 1]; rw [Nat.zero_add]\n  /-\n    g : Nat → Nat\n    y : Nat\n    h : let x := y + 1;\n        g x = x\n    ⊢ g (y + 1) = y + 1\n  -/\n  exact h\n* 添加 zeta conv 策略，用于展开 let 声明。例如：example (h : let x := y + 1; 0 + x = y) : False := by\n  conv at h => zeta; rw [Nat.zero_add]\n  /-\n    y : Nat\n    h : y + 1 = y\n    ⊢ False\n  -/\n  simp_arith at h\n* 改进命名空间解析。参见问题 #1224。例如：import Lean\nopen Lean Parser Elab\nopen Tactic -- now opens both `Lean.Parser.Tactic` and `Lean.Elab.Tactic`\n* 将 constant 命令重命名为 opaque。参见 Zulip 上的讨论。* 扩展 induction 与 cases 语法：单个分支中可包含多个左侧模式。这一扩展与 match 表达式上的实现非常相似。例如：inductive Foo where\n  | mk1 (x : Nat) | mk2 (x : Nat) | mk3\n\ndef f (v : Foo) :=\n  match v with\n  | .mk1 x => x + 1\n  | .mk2 x => 2*x + 1\n  | .mk3   => 1\n\ntheorem f_gt_zero : f v > 0 := by\n  cases v with\n  | mk1 x | mk2 x => simp_arith!  -- New feature used here!\n  | mk3 => decide\n* 现在支持 do 块中的 let/if 缩进。* 添加无名 antiquotation $_，用于语法 quotation 模式。* 添加未使用变量检查器。欢迎反馈！* 如果声明体中包含一个既未出现在声明类型中、也不是显式参数的 universe 参数，Lean 现在会报错。\n例如：/-\nThe following declaration now produces an error because `PUnit` is universe polymorphic,\nbut the universe parameter does not occur in the function type `Nat → Nat`\n-/\ndef f (n : Nat) : Nat :=\n  let aux (_ : PUnit) : Nat := n + 1\n  aux ⟨⟩\n\n/-\nThe following declaration is accepted because the universe parameter was explicitly provided in the\nfunction signature.\n-/\ndef g.{u} (n : Nat) : Nat :=\n  let aux (_ : PUnit.{u}) : Nat := n + 1\n  aux ⟨⟩\n* 添加 subst_vars 策略。* 修复多重继承中结构体字段里的 autoParam 丢失问题。。* 添加 [eliminator] 属性。它允许用户为 induction 与 cases 策略指定默认的递归子/消去子。\n这是 using 记法的另一种替代方式。例如：@[eliminator] protected def recDiag {motive : Nat → Nat → Sort u}\n    (zero_zero : motive 0 0)\n    (succ_zero : (x : Nat) → motive x 0 → motive (x + 1) 0)\n    (zero_succ : (y : Nat) → motive 0 y → motive 0 (y + 1))\n    (succ_succ : (x y : Nat) → motive x y → motive (x + 1) (y + 1))\n    (x y : Nat) :  motive x y :=\n  let rec go : (x y : Nat) → motive x y\n    | 0,     0 => zero_zero\n    | x+1, 0   => succ_zero x (go x 0)\n    | 0,   y+1 => zero_succ y (go 0 y)\n    | x+1, y+1 => succ_succ x y (go x y)\n  go x y\ntermination_by go x y => (x, y)\n\ndef f (x y : Nat) :=\n  match x, y with\n  | 0,   0   => 1\n  | x+1, 0   => f x 0\n  | 0,   y+1 => f 0 y\n  | x+1, y+1 => f x y\ntermination_by f x y => (x, y)\n\nexample (x y : Nat) : f x y > 0 := by\n  induction x, y <;> simp [f, *]\n* 为结构递归与良基递归模块添加对 casesOn 应用的支持。\n这一特性在使用策略编写定义时非常有用。例如：inductive Foo where\n  | a | b | c\n  | pair: Foo × Foo → Foo\n\ndef Foo.deq (a b : Foo) : Decidable (a = b) := by\n  cases a <;> cases b\n  any_goals apply isFalse Foo.noConfusion\n  any_goals apply isTrue rfl\n  case pair a b =>\n    let (a₁, a₂) := a\n    let (b₁, b₂) := b\n    exact match deq a₁ b₁, deq a₂ b₂ with\n    | isTrue h₁, isTrue h₂ => isTrue (by rw [h₁,h₂])\n    | isFalse h₁, _ => isFalse (fun h => by cases h; cases (h₁ rfl))\n    | _, isFalse h₂ => isFalse (fun h => by cases h; cases (h₂ rfl))\n* Option 再次成为 monad。辅助类型 OptionM 已被移除。参见 Zulip 讨论串。* 改进 split 策略。它过去会在形如 match h : e with ... 且 e 不是自由变量的 match 表达式上失败。\n这种失败过去发生在泛化阶段。* 为在判别式中使用 h : 记法的 match 表达式引入新的编码。相关信息在反精译过程中不会丢失，\n这也为更好的 split 策略奠定了基础。例如：#print Nat.decEq\n/-\nprotected def Nat.decEq : (n m : Nat) → Decidable (n = m) :=\nfun n m =>\n  match h : Nat.beq n m with\n  | true => isTrue (_ : n = m)\n  | false => isFalse (_ : ¬n = m)\n-/\n* exists 策略现在接受以逗号分隔的项列表。* 添加 dsimp 与 dsimp! 策略。它们保证结果项在定义上相等，并且只应用\nrfl 定理。* 修复在 match 模式中使用带 [matchPattern] 标记的定义（例如 Nat.add）时的绑定器信息。\n现在，下面示例中的变量 y 会具有正确的绑定器信息。def f (x : Nat) : Nat :=\n  match x with\n  | 0 => 1\n  | y + 1 => y\n* （修复）结构体字段的默认值现在可以依赖结构体参数。例如：structure Something (i: Nat) where\nn1: Nat := 1\nn2: Nat := 1 + i\n\ndef s : Something 10 := {}\nexample : s.n2 = 11 := rfl\n* 在 simp 所使用的 dsimp 辅助方法中应用 rfl 定理。dsimp 可以在表达式中的任意位置使用，\n因为它保持定义相等。* 细化 auto bound implicit 特性。它不再把与当前正在定义的声明同名的未绑定变量纳入考虑。例如：def f : f → Bool := -- Error at second `f`\n  fun _ => true\n\ninductive Foo : List Foo → Type -- Error at second `Foo`\n  | x : Foo []\n在这一细化之前，上述声明会被接受，而第二个 f 与 Foo 会被视作 auto implicit 变量。也就是说，\nf : {f : Sort u} → f → Bool，以及\nFoo : {Foo : Type u} → List Foo → Type。* 修复递归声明的语法高亮。例如inductive List (α : Type u) where\n  | nil : List α  -- `List` is not highlighted as a variable anymore\n  | cons (head : α) (tail : List α) : List α\n\ndef List.map (f : α → β) : List α → List β\n  | []    => []\n  | a::as => f a :: map f as -- `map` is not highlighted as a variable anymore\n* 为 Lean.Meta.Simp.Config 添加 autoUnfold 选项，以及以下宏* simp! 对应 simp (config := { autoUnfold := true })* simp_arith! 对应 simp (config := { autoUnfold := true, arith := true })* simp_all! 对应 simp_all (config := { autoUnfold := true })* simp_all_arith! 对应 simp_all (config := { autoUnfold := true, arith := true })当 autoUnfold 设为 true 时，simp 会尝试展开以下几类定义* 由结构递归定义的递归定义。* 函数体为 match 表达式的非递归定义。此类定义\n仅在 match 可归约时才会被展开。\n例如：def append (as bs : List α) : List α :=\n  match as with\n  | [] => bs\n  | a :: as => a :: append as bs\n\ntheorem append_nil (as : List α) : append as [] = as := by\n  induction as <;> simp_all!\n\ntheorem append_assoc (as bs cs : List α) : append (append as bs) cs = append as (append bs cs) := by\n  induction as <;> simp_all!\n* 添加 save 策略，以更方便地创建 checkpoint。例如：example : <some-proposition> := by\n  tac_1\n  tac_2\n  save\n  tac_3\n  ...\n等价于example : <some-proposition> := by\n  checkpoint\n    tac_1\n    tac_2\n  tac_3\n  ...\n* 移除对归纳数据类型构造子中 {} 注解的支持。这一注解几乎无人使用，而参数绑定的绑定器信息现在可通过新的“归纳族索引提升为参数”机制来控制。例如，下面这个使用 {} 的声明inductive LE' (n : Nat) : Nat → Prop where\n  | refl {} : LE' n n -- Want `n` to be explicit\n  | succ  : LE' n m → LE' n (m+1)\n现在可以写成inductive LE' : Nat → Nat → Prop where\n  | refl (n : Nat) : LE' n n\n  | succ : LE' n m → LE' n (m+1)\n在这两种写法中，该归纳族都有一个参数和一个索引。\n请记住，实际参数个数可通过命令 #print 查看。* 移除 structure 命令中对 {} 注解的支持。* 对 LSP 服务器做了多项改进。例如：在互递归片段中支持“跳转到定义”、修复 match 表达式模式中的错误悬停信息、支持模式变量的“跳转到定义”、修复函数头中的自动补全，等等。* 在 macro ... xs:p* ... 及类似的组合子宏绑定中，xs 现在具有正确的类型 Array Syntax* 语法模式中的标识符在匹配时现在会忽略宏作用域。* 改进构造子 auto implicit 参数的绑定器名称。例如，给定归纳数据类型inductive Member : α → List α → Type u\n  | head : Member a (a::as)\n  | tail : Member a bs → Member a (b::bs)\n之前：#check @Member.head\n-- @Member.head : {x : Type u_1} → {a : x} → {as : List x} → Member a (a :: as)\n现在：#check @Member.head\n-- @Member.head : {α : Type u_1} → {a : α} → {as : List α} → Member a (a :: as)\n* 改进当构造子参数 universe level 过大时的错误消息。* 添加对 for h : i in [start:stop] do ..  的支持，其中 h : i ∈ [start:stop]。这一特性有助于证明如下函数的终止性：inductive Expr where\n  | app (f : String) (args : Array Expr)\n\ndef Expr.size (e : Expr) : Nat := Id.run do\n  match e with\n  | app f args =>\n    let mut sz := 1\n    for h : i in [: args.size] do\n      -- h.upper : i < args.size\n      sz := sz + size (args.get ⟨i, h.upper⟩)\n    return sz\n* 添加策略 case'。它与 case 类似，但在失败时不会自动承认目标。\n例如，当我们在 first | ... | ... 中需要使用 case'，并希望在 case' 失败时尝试下一个分支时，这个新策略就很有用。* 添加策略宏macro \"stop\" s:tacticSeq : tactic => `(repeat sorry)\n参见 Zulip 上的讨论。* 显示目标时，如果不可访问的命题名没有前向依赖，我们就不再显示它们的名称，\n但仍会显示它们的类型。\n例如，目标case node.inl.node\nβ : Type u_1\nb : BinTree β\nk : Nat\nv : β\nleft : Tree β\nkey : Nat\nvalue : β\nright : Tree β\nihl : BST left → Tree.find? (Tree.insert left k v) k = some v\nihr : BST right → Tree.find? (Tree.insert right k v) k = some v\nh✝ : k < key\na✝³ : BST left\na✝² : ForallTree (fun k v => k < key) left\na✝¹ : BST right\na✝ : ForallTree (fun k v => key < k) right\n⊢ BST left\n现在会显示为case node.inl.node\nβ : Type u_1\nb : BinTree β\nk : Nat\nv : β\nleft : Tree β\nkey : Nat\nvalue : β\nright : Tree β\nihl : BST left → Tree.find? (Tree.insert left k v) k = some v\nihr : BST right → Tree.find? (Tree.insert right k v) k = some v\n : k < key\n : BST left\n : ForallTree (fun k v => k < key) left\n : BST right\n : ForallTree (fun k v => key < k) right\n⊢ BST left\n* by_cases 策略中的假设名现在是可选的。* 修复 syntax 与 kind 名称之间的不一致。\n节点种类 numLit、charLit、nameLit、strLit 和 scientificLit 现在分别改名为\nnum、char、name、str 与 scientific。例如，我们现在写作macro_rules | `($n:num) => `(\"hello\")\n而不是macro_rules | `($n:numLit) => `(\"hello\")\n* （实验性）为大型交互式证明添加新的 checkpoint <tactic-seq> 策略。* 将策略 nativeDecide 重命名为 native_decide。* 现在任何语法中都接受 antiquotation。因此，incQuotDepth syntax 解析器已经过时并被移除。* 已将策略 nativeDecide 重命名为 native_decide。* 在精译 match 分支右侧之前，先“清理”局部上下文。例如：example (x : Nat) : Nat :=\n  match g x with\n  | (a, b) => _ -- Local context does not contain the auxiliary `_discr := g x` anymore\n\nexample (x : Nat × Nat) (h : x.1 > 0) : f x > 0 := by\n  match x with\n  | (a, b) => _ -- Local context does not contain the `h✝ : x.fst > 0` anymore\n* 改进 let 模式（以及 have 模式）的宏展开。在下面的例子中，example (x : Nat × Nat) : f x > 0 := by\n  let (a, b) := x\n  done\n生成的目标现在是 ... |- f (a, b) > 0，而不是 ... |- f x > 0。* 添加交叉编译的 aarch64 Linux 与 aarch64 macOS 发布版本。* 在文档中加入教程风格的示例，使用 LeanInk+Alectryon 渲染。\n\n","context":"Lean 中文参考手册\u0009发行说明","header":"Lean 4.0.0-m5 (2022-08-22)","id":"/releases/v4.0.0-m5/#release-v4___0___0-m5"},"/releases/v4.24.0/#The-Lean-Language-Reference--Release-Notes--Lean-4___24___0-_LPAR_2025-10-14_RPAR_--Highlights--invariants-and-with-sections-in-mvcgen":{"contents":"* #9927 为 mvcgen 实现了受 induction 启发的扩展语法，允许可选的 invariants 和 with 小节。\n\n下面的示例证明了 nodup 能正确检查列表中的重复元素。\n\nimport Std.Tactic.Do\nimport Std\n\nopen Std Do\n\ndef nodup (l : List Int) : Bool := Id.run do\n  let mut seen : HashSet Int := ∅\n  for x in l do\n    if x ∈ seen then\n      return false\n    seen := seen.insert x\n  return true\n\ntheorem nodup_correct (h : nodup l = r) : r = true ↔ l.Nodup := by\n  unfold nodup at h\n  apply Id.of_wp_run_eq h; clear h\n  mvcgen\n  invariants\n  · Invariant.withEarlyReturn\n      (onReturn := fun ret seen => ⌜ret = false ∧ ¬l.Nodup⌝)\n      (onContinue := fun xs seen =>\n        ⌜(∀ x, x ∈ seen ↔ x ∈ xs.prefix) ∧ xs.prefix.Nodup⌝)\n  with grind\n\n\n","context":"Lean 中文参考手册\u0009发行说明\u0009Lean 4.24.0 (2025-10-14)\u0009亮点","header":"mvcgen  中的  invariants  与  with  小节","id":"/releases/v4.24.0/#The-Lean-Language-Reference--Release-Notes--Lean-4___24___0-_LPAR_2025-10-14_RPAR_--Highlights--invariants-and-with-sections-in-mvcgen"},"/releases/v4.31.0/#The-Lean-Language-Reference--Release-Notes--Lean-4___31___0-_LPAR_2026-06-13_RPAR_--Highlights--Lake___-Built-in-Linting":{"contents":"Lake 获得了内置的检查框架，可通过 lake lint 标志（#13393、#13431）访问。它附带了来自 Batteries/Mathlib 上游的环境检查器（defLemma/defProp、checkUnivs） - 另请参阅 #13356 中的核心上游 - 以及 builtinLint 包配置选项。标志包括 --builtin-lint、--builtin-only、--clippy、--lint-all 和 --lint-only <name>，并且 @[builtin_nolint] 属性抑制每个声明的特定检查器。\n\n#13513 通过将警告保留到每个模块的 .olean 中，将其扩展到 text 检查器，而 #13843 使模块系统目标检查其公共表面，与下游消费者所看到的相匹配。\n\n","context":"Lean 中文参考手册\u0009发行说明\u0009Lean4.31.0 (2026-06-13)\u0009亮点","header":"Lake：内置 Linting","id":"/releases/v4.31.0/#The-Lean-Language-Reference--Release-Notes--Lean-4___31___0-_LPAR_2026-06-13_RPAR_--Highlights--Lake___-Built-in-Linting"}});